"use strict";

// ライブラリ用のオブジェクトの作成
if (window.game === undefined) {window.game = {}}
if (window.game.core === undefined) {window.game.core = {}}

(function() {
  var _t = game.core; //ショートカットキーの作成
  
})